# Experiments

This directory contains research and probe code that helps decide what the product should trust.

- `probes/`: runnable spike scripts for OpenAI API usage, local source discovery, and usage event normalization.
- `fixtures/`: synthetic data that exercises those probes without reading real user prompts, responses, transcripts, or code.

Keep experimental code here until it has a stable contract and belongs in `backend/`.
