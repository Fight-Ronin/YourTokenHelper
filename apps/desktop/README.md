# Desktop App

The desktop app implementation will live here.

Expected V1 surface:

- daily usage view;
- rolling 7-day weekly usage view;
- source health and setup states;
- secondary API cost page for OpenAI API/Admin data.

This directory should stay UI-focused. Shared source parsing and aggregation logic belongs in `backend/`.
